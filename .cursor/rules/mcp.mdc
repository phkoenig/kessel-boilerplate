---
description: MCP Server Governance - ein Supabase-MCP pro Workspace, Context7 für Library-Docs.
applyIntelligently: true
---

# MCP Governance

Dieses Projekt nutzt **genau einen Supabase-MCP** pro Cursor-Workspace.

## Kernprinzip

**Ein MCP pro Cursor-Workspace** fuer Supabase - und dieser zeigt immer auf die App-Supabase der aktuellen Ableitung.

---

## Regeln

### 1. Ein Supabase-MCP pro Workspace

- Jedes Cursor-Projekt hat **maximal einen** Supabase-MCP konfiguriert
- Dieser zeigt immer auf die aktive App-Supabase
- Keine parallelen Supabase-MCP-Instanzen

### 2. Andere DBs via API/SDK

Alle anderen Supabase-Datenbanken werden angesprochen über:

- Backend-API-Routes (`/api/...`)
- Supabase JavaScript SDK (`@supabase/ssr`)
- Server-Side Scripts (`scripts/*.mjs`)

**NIEMALS** über zusätzliche MCP-Instanzen im selben Workspace!

### 3. Keine weiteren Supabase-MCPs aktivieren

```
❌ VERBOTEN: Mehrere Supabase-MCPs in einem Workspace
✅ ERLAUBT: Ein Supabase-MCP + andere MCPs (Context7, Shadcn, etc.)
```

---

## Warum diese Architektur?

Cursor hat bekannte Bugs beim Routing von Requests auf mehrere MCP-Instanzen desselben Typs:

- Requests werden unzuverlässig geroutet
- Falsche Datenbank-Instanz wird angesprochen
- Authentifizierung kann fehlschlagen

---

## Konfiguration

```json
// .cursor/mcp.json
{
  "mcpServers": {
    "supabase_KESSEL": {
      "type": "http",
      "url": "https://mcp.supabase.com/mcp?project_ref=PROJECT_REF"
    }
  }
}
```

---

## Für AI-Assistenten

1. **Nutze den konfigurierten MCP** für die aktive Dev-DB
2. **Schlage KEINE weiteren Supabase-MCPs vor**
3. **Für andere DBs:** Verwende API-Routes oder SDK-Code

```typescript
// ✅ RICHTIG: SDK für andere DBs
import { createClient } from "@supabase/supabase-js"
const infraClient = createClient(INFRA_URL, INFRA_KEY)

// ❌ FALSCH: Nicht vorschlagen, weiteren MCP zu konfigurieren
```

---

## Context7 MCP für Library-Dokumentation

Context7 ist für **externe Library-Dokumentation** (React, Supabase, Next.js, AI SDK, etc.).

### Wann Context7 nutzen:
- Aktuelle API-Referenzen für externe Libraries abrufen
- Breaking Changes oder neue Features einer Library prüfen
- "Wie funktioniert X in Library Y?" Fragen

### Was Context7 NICHT kann:
- Projekt-Code analysieren (dafür: `codebase_search`, `grep`)
- Abhängigkeiten zwischen eigenen Dateien finden
- Impact-Analyse im eigenen Projekt

### Empfehlung
Bei Fragen zu externen Libraries Context7 nutzen, um aktuelle Dokumentation zu erhalten - besonders bei Libraries die sich schnell ändern (React, Next.js, Supabase, Clerk, SpacetimeDB).
